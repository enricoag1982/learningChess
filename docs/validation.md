# Validation — Chess for Kids

Related: [roadmap.md](roadmap.md), [../CONTRIBUTING.md](../CONTRIBUTING.md).

Every merged iteration gets an annotated tag `m<N>.<i>` (milestone done: also `m<N>`), created on merge by `.github/workflows/tag.yml`. Tag message = the tag's row in §2: scope, checks run (IDs below), notes.

## 1. Checks

| ID | Check | Verifies |
|---|---|---|
| F | Format | Prettier: code, YAML, JSON |
| L | Lint | ESLint `strictTypeChecked`; layer rules (domain pure, chess.js behind adapter) |
| T | Typecheck | `tsc` strict, every package |
| U | Unit + content tests | Vitest: rules, parsers, content files, adapters, components |
| B | Build | Content JSON + web bundle |
| E | E2E | Playwright on production build (Chromium) |
| CI | CI `quality` | F L T U B E on GitHub Actions, PR head |
| C | Clean install | Fresh clone → `pnpm install --frozen-lockfile` → all scripts pass |
| P | Perft | Move generator node counts vs published values |
| N | Negative | Deliberately broken input rejected with a clear message |
| S | Screenshot | App run in a browser, screens checked by eye (tablet 1024×768, phone 390×844) |
| O | Offline | Built app reloads with network off |
| Z | Size | Initial JS gzipped vs budget (≤ 300 KB) |
| D | Deploy | Pages URL serves the app |
| A | Accessibility | axe-core scan of every lesson screen (serious / critical = fail); kid targets ≥ 64 px on desktop, tablet, iPad portrait, phone |

## 2. Log

| Tag | Date | Scope | Checks | Notes |
|---|---|---|---|---|
| `m0.1` | 2026-09-24 | Workspace + tooling; core chess (diagram / FEN parser, `ChessRules` over chess.js); content locale pipeline | F L T U B CI C P N | U: 60 tests (core 42, content 18). P: 5 reference positions match published perft counts (kept as unit test). N: bad diagram, bad FEN, two white kings, empty text, bad key, duplicate key → rejected with file / row / key in message. C: fresh clone of branch green. Lint rules checked to fire (chess.js import, React in domain) |
| `m0.2` | 2026-09-24 | Web PWA shell (Vite, React 19, Tailwind tokens from sketches, self-hosted fonts, typed i18n); localStorage store (versioned, migrations) + profile repository; Playwright smoke + offline; CI e2e; Pages deploy + tag workflows | F L T U B E CI S O Z N | U: 72 tests (core 42, content 18, web 12). E: 4 (desktop + tablet 1024×768). S: shell at 1024×768 and 390×844 checked. O: offline reload passes; 8 font files precached. Z: initial JS 84 KB gz (budget 300). N: unknown i18n key fails typecheck. Tag workflow dry run: title → tag, backfill, missing row → error |
| `m0` | 2026-09-24 | M0 scaffold done | CI D | Exit: CI green; rules tests pass (incl. perft). D pending: repo setting Pages → Source = GitHub Actions (owner only); deploy runs on every push to `master` |
| `m1.2` | 2026-09-24 | Core exercise engine: variant rules (walls, static opponent), `collect-stars`, `select-squares`, `capture`, stars, hint ladder, BFS solver, piece-vs-static mini-game | F L T U CI N | U: 77 core tests. Manual: doc example `rook-02` → optimal 3 (a5, e5, e8) = doc `stars3`; hint ladder piece → target → move, level 3 caps at 1 star; 8 stars + walls solved (14 moves) in 243 ms. Review fix: solver replays castling through rules (test red before fix, green after) |
| `m1.3` | 2026-09-24 | Lesson content pipeline (lesson / exercise / mini-game YAML → Zod → semantic checks → `content.json`); Rook lesson (story, demo, 2 guided, 8 exercises) + Hungry Rook | F L T U B CI N | U: 131 tests (core 77, content 42, web 12). Content checks: boards valid, solvable, `stars3` = solver optimum (rook-01..08: 2, 3, —, 3, 1, —, 3, 5), Hungry Rook par 7 = optimum, keys present. N: `stars3` below optimum, walled-in star, unknown text key → all 3 reported in one build run. Review: lesson texts read for an 8-year-old; one text fix |
| `m1.1` | 2026-09-24 | Board UI: own SVG piece set, stars / rocks, tap-tap + drag, highlights (selection, hint, wrong, last move), move animation, screen-reader grid; also M1.4a (non-UI): progress domain, lesson steps, ports + use cases, localStorage progress, Web Speech narrator, bundled content | F L T U B E CI S | U: 203 tests (core 106, content 42, web 55). E: 4. S: piece gallery at 3× and board at 1024×768 reviewed → pieces redrawn (bigger, horse-head knight, single-slit bishop). Manual Playwright on dev playground: mouse drag d4→d7 capture, tap-tap select → target dots → move with mouse and touch. Z: initial JS 85 KB gz |
| `m1.4` | 2026-09-24 | Lesson flow UI: Home → Story → Demo → Try → Exercises → Boss → Complete; Zustand app store; narration on every text + replay; autosave after each step, resume at the unfinished step | F L T U B E CI S N | U: 214 tests (core 106, content 42, web 66). E: 8 (desktop + tablet): full Rook lesson played with solver lines incl. boss within par → "Lesson complete!" → Home stars; close mid-lesson + reload → resumes at exercise 3; offline reload. S: 7 screens × tablet / phone reviewed. Manual on production build: 0 external requests from the app; narration called for every step text; profile, progress, attempts in localStorage. Z: JS 112 KB gz. Found for m1.5: phone top bar / Story cramped; hint and feedback replace the instruction; guided tries show stars |
| `m1.5` | 2026-09-24 | Polish: phone top bar (phase chip), phone Story, instruction kept with hint / feedback notes, no stars on guided tries, board labels via i18n, iPad portrait layout, plural text keys; checks: axe scan, target sizes on 4 viewports, no external requests, offline lesson, reduced motion, size budget in CI | F L T U B E CI S A O Z N | U: 218 tests (core 106, content 44, web 68). E: 16 (desktop, tablet, iPad portrait, phone). A: 1 axe violation fixed (stars pill label). Review found "Say it again" squeezed to 27 px on stacked layouts → fixed (`shrink-0`); target check extended to hint state (red before fix, green after); Story content clipped on phone → safe centering. Z: 110.8 KB gz, now a CI step. O: lesson playable offline after first load |
| `m1` | 2026-09-24 | M1 vertical slice done | E CI A O | Exit: Rook lesson end to end (story → demo → guided → 8 exercises → Hungry Rook → complete) on tablet layout, progress saved + resume (e2e). Pending (user): playtest 1; Pages source = GitHub Actions |
| `m2.1` | 2026-09-24 | Profiles (picker, new player: nickname + 8 animal avatars), first run (welcome → parent password → file copy → first child), parent password screen (reminder of file location, 5 wrong → 1 min wait), parent area (children, rename, avatar, delete with confirm, add, change password) | F L T U B E CI S A Z | U: 273 tests (core 136, content 44, web 93). E: 22 (profiles flow incl. download + 60 s lockout via fake clock; all earlier specs through the new start flow; a11y on new screens). S: 13 screens × desktop / phone reviewed. Manual: real download `chess-for-kids-parent-password.txt` contains the password; M1 installs (profile, no lock) only asked for the password (unit test). Z: 117 KB gz |
| `m2.2` | 2026-09-24 | Exercise types `yes-no`, `choice`, `best-move`, `setup`: engine (answers, wrong moves bounce back, piece placement, stars, hint ladders), content schema + loader checks (legal solutions, setup start ⊆ target, option keys), UI (Yes / No, option tiles, bounce animation, setup tray); dev playground `/#exercises` | F L T U B E CI S N Z | U: 360 tests (core 190, content 63, web 107). E: 22. S: 4 types × initial / wrong / solved × desktop / phone. Review fixes: Yes was styled as primary (biased the answer) → both neutral, RTL guard; choice options 40 px → 96 px tiles, RTL guard; animated piece painted under later squares → z-index. Open (→ m2.4): setup tray below the fold on phone. Z: 120 KB gz |
| `m2.3` | 2026-09-24 | World 2 lessons Bishop (Elephant), Queen (Lioness), King (Lion), Knight (Horse): story, demo, 2 guided, 6–8 exercises each; bosses Hungry Bishop, Hungry Queen, King Walk, Knight Maze (static mini-game goal `collect-stars`); character art placeholders; dev lesson preview `/#lesson=<id>` | F L T U B E CI S N Z | U: 1 playthrough test replays every lesson and boss through the engine with solver lines; all `stars3` / par = solver optimum. E: 22 (Rook flow unchanged). S: story, demo, exercises, bosses per lesson reviewed. Review fix: knight exercises reordered so difficulty rises (6-move "star next to the Horse" moved last). Z: 123 KB gz |
| `m2.5` | 2026-09-24 | Journey map (worlds list + paths, habitat map with winding path, lesson nodes done / current / locked with stars and boss flag, locked-tap message, coming-soon worlds), Home: next lesson ("Today you meet …"), rank + stars pills, Journey tile; tracks catalog + availability rules (merged earlier as part 1); generic e2e `playLesson` for all 7 exercise types + static bosses | F L T U B E CI S A Z | U: 446 tests (core 224, content 108, web 114). E: 24 (first lesson computed from content, journey unlock via stored progress, a11y + targets incl. Journey). S: Home (new / in progress / all done), Journey (current, in progress, coming soon) × 3 viewports reviewed. Z: 127 KB gz |
| `m2.4` | 2026-09-24 | World 1 lessons Squares (yes-no, select-squares), Lines (rows, columns, diagonals), Setup (setup, queen on her colour) taught by Owl; series mini-games Square Hunt (6 rounds) and Setup Race (2 rounds); demo highlight of explicit squares; setup tray under the board on stacked layouts; Journey / Home name Owl-taught lessons by title | F L T U B E CI S A N Z | U: 463 tests (core 231, content 113, web 119) incl. World 1 playthrough. E: 24; journey now starts at Squares, all e2e content-driven; a11y walk asserts every exercise type + series and static boss scanned. N (manual content audit vs boards): all light / dark and line answers correct; fixed a leading question ("light too?"), two ambiguous diagonal rounds (king on two diagonals), two ambiguous setup squares (d1 / e1, d1 / f1). Z: 130 KB gz |
| `m2.6` | 2026-09-24 | Play vs Mouse: bot in a Web Worker (in-thread fallback), `versus` mini-game mode (kid colour, win conditions, par, take back, danger ring, legal-move dots, narrated bot moves); Pawn (Caterpillar) and Promotion lessons; Pawn Wars Jr. (4 pawns) and Pawn Wars (8 pawns) vs Mouse; bot engine core merged earlier (part 1) | F L T U B E CI S A N Z P | U: all green (core, content, web). E: 26 incl. full versus games in the a11y walk and a seeded Pawn Wars test (bot reply < 3 s). P (performance): worker round trip + Mouse move ≈ 80–100 ms in the browser (target ≤ 300 ms). N (content audit): answers correct; fixed 2 wordings (diagonal capture, blocked promotion square) and 2 exercises that duplicated their guided boards. S: Pawn story / exercise, Promotion choice, Pawn Wars (danger ring, win, loss) reviewed. Z: 151.5 KB gz incl. 15.5 KB worker |
| `m2.7` | 2026-09-24 | Play screen (vs Computer card with levels, full game locked until World 4, vs Friend coming soon, mini-games grid: locked / unlocked with best stars, standalone sessions for static / series / versus), My Den (rank ladder, animal friends), Home tiles (Journey, Play, My Den); mini-game progress | F L T U B E CI S A Z | U: all green. E: 28 (Play: replay Hungry Rook → 3 stars on tile; Den: friend + rank; a11y walk incl. Play + Den). Review fixes: a lesson boss logged two attempts (scored + unscored) → one attempt, test asserts it; "Pawn Wars: After Pawn lesson" (wrong: unlocks after Promotion) → label by lesson title when the character repeats. Z: 155 KB gz |
| `m2` | 2026-09-24 | M2 Worlds 1–2 done | E CI A | Exit: Worlds 1–2 (10 lessons, 9 mini-games) playable end to end (content playthrough tests + e2e walks), profiles + parent password, Journey, Mouse bot, Play, My Den. Pending (user): playtest 2 (and 1); Pages source = GitHub Actions |
| `m3.1` | 2026-09-24 | Mate-in-n exercise (scripted replies, any mate accepted), select-squares `attacked-by` / `check-escapes`, rule facts (attacked, defended, hanging, check, mate, stalemate), yes-no / choice `verify` checked at build, check ring on the board | F L T U B E CI S Z | U: core 295, content 139 (valid + failing fixture per new loader rule), web 136 (any mate accepted, scripted reply narrated, wrong move bounce, check ring label). E: 28. S: mate-in-2 mid-line (check ring, reply shown), check-escapes selection at 1024×768 and 390×844 reviewed. Z: 156 KB gz |
| `m3.2` | 2026-09-24 | World 3 (Attack & Defence): lessons Attack, Defend, Safe or Not?, Piece Values, Trades (40 exercises, all answers rule-verified at build); mini-games Queen vs Pawns, Safe or Not? (10-round series), Army Battle, world boss Win the Queen; world-level boss in the catalog (Journey crown node, Home "Today", world mastery); new verify kinds (attack, save, take-free, good-trade, worth, trade) | F L T U B E CI S A N Z P | U: core 319, content 186 (fixture per verify rule, World 3 playthrough, winnability), web 142. E: 32 (Attack lesson to its boss, Safe or Not? from Play). P (playability, seeded bot stand-ins, 20–30 seeds): Queen vs 8 pawns too hard (bear 47%, rabbit < 30%) → 4 pawns: rabbit 90%, bear 90%; Army Battle, Win the Queen: fox 90%. N (content audit vs boards, lead): fixed defend-05 (accepted blocks that hang the bishop), safe-pieces-05 and series r7 ("defended" piece attacked by a cheaper one called safe; new build rule forbids it), duplicate boards (safe-pieces-01/07, trades-01, trades-g2), illegal pawn on a8 (series r10). S: Journey boss node locked / available / won; phone map nodes overlapped (Horse over Lion) → zig-zag layout; exercise and mini-game screens at 1024×768 and 390×844. Z: 160.8 KB gz |
| `m3.3` | 2026-09-24 | World 4 (Check & Mate): lessons Check, Escape Check, Checkmate, Mate in 1, Stalemate (44 exercises, rule-verified); series Escape the Check, Mate Hunt (10 rounds each); world boss: first full game vs Mouse; verify kinds check / escape-king / escape-block / escape-capture, mate-in-n `trap: stalemate`; check ring in versus games | F L T U B E CI S A N Z P | U: core 329, content 222 (World 4 playthrough, fixture per verify rule), web 155 (versus: check ring, mate win, stalemate draw, castling, en passant, promotion). E: 38 (Mate in 1 lesson + Mate Hunt end to end; first-game smoke with bot replies < 3 s; a11y walk crosses world bosses). P: first-game fox stand-in vs Mouse 9/10 wins by mate, median 51 kid moves. N (lead audit vs boards): check-07 accepted queen checks the king can capture (Qg7, Qg8) → new position, only safe checks; escape-check-07 block left the bishop hanging → blocking square now defended; 3 positions without a white king → king added; check-03 wording ("right next to" for a knight check); 3 series rounds copied lesson exercises → new positions. S: exercises per lesson, series starts, full game start + mid-game check at 1024×768 and 390×844. Z: 165 KB gz |
| `m3.4` | 2026-09-24 | Concept mastery + Leitner review scheduler (`ConceptStats`, box moves, warm-up / Practice task picking); Today session (Home "Start today": due warm-up → Journey next step → one mini-game → summary with stars / new friends / rank); Practice screen (daily warm-up card, topic list with accuracy dots + weak tag, 5-task topic run); storage schema v2 (`concept-stats` record + migration) | F L T U B E CI S A Z | U: core 380, content 190 (unchanged), web 161. E: 38 (Today session: seeded due concept → 3 warm-up tasks → next lesson; Practice topic run of 5; whole-curriculum a11y walk now scanned Practice + session summary too). Review fix: the immediate review-entry trigger first read any non-first-try `Attempt` (`correct: false`) — even a single stray error — which the a11y walk's deliberate wrong-state scanning showed was too eager (a review-only session after almost every lesson); narrowed to `errors >= EASIER_AFTER_ERRORS` (2), the same threshold the easier-variant offer itself uses ("failed an exercise twice"), with a unit test pinning the boundary. A session's trailing mini-game pick also excluded the world boss already playing as that session's other activity (would otherwise double up). S: Home (Start today), warm-up task, Practice (weak topic), session summary at 1024×768 and 390×844 reviewed. Z: 165.4 KB gz Lead review: Practice / session-summary Owl row squeezed to 4 short lines on phones → stacked like Home (re-shot at 390×844). |
| `m3.5` | 2026-09-24 | Play → vs Computer: level chips (Mouse/Rabbit real unlock rules, Fox/Wolf/Bear shown locked with condition, M4 unlocks them), "Full game" button reuses the `first-game`/`VersusStep` UI at the chosen level; `GameRecord` (new `GameRecordRepository` port + localStorage adapter, storage schema v3) saved for every full game and `versus` mini-game, win/loss/draw/abandoned; draw screen now names which draw (stalemate / insufficient material / threefold / 50-move); Owl's mate hint after 60 kid moves (`mateHint`, depth-2 search, highlights piece + target); "Stop game?" leave confirm on the full-game screen; Journey node label fix for a lesson whose character repeats (Pawn "Caterpillar" / Promotion "Caterpillar Transforms!"); My Den games-won total | F L T U B E S Z | U: core 392 (+12: `games.ts`/`hint.ts` + versus/minigames additions), content 222 (unchanged), web 175 (+13: level chips, full-game start/win/leave-abandoned, draw-reason text). E: 44 across chromium/tablet/tablet-portrait/phone (20 specs × the two content projects, a11y × all 4) — new: Play → Full game vs Mouse smoke test (3 kid moves < 3s, leave → `GameRecord` `abandoned`); a11y walk now also scans the full-game screen (start / mid-game / leave-confirm) once World 4 is mastered. S: Play vs Computer card (Mouse unlocked "not played yet", Rabbit locked "Beat Mouse 3 times"), full-game mid-game with the check ring, result win (2 stars, checkmate) and draw ("The same position happened three times"), Journey World 2 (Caterpillar / Caterpillar Transforms! distinct labels) at 1024×768 and 390×844 reviewed. Z: 172.4 KB gz. Deviation: `GameRecord`'s in-memory/localStorage split follows the existing per-repository pattern (one class, `createMemoryStorage()` in tests vs real `localStorage` in the app) rather than two hand-written classes. |
| `m3.5` | 2026-09-24 | Full game vs computer in Play (World 4 mastered): level chips (Mouse; Rabbit after beating Mouse 3×; Fox–Bear locked with conditions), full-game screen (check ring, draw reasons, mate hint after 60 moves, leave = abandoned), `GameRecord` for full games and every versus mini-game (storage v3), games won in My Den; Journey labels by lesson title for repeated characters | F L T U B E CI S A Z | U: core 393, content 222, web 175 (level chips, full game win → record → "Won 1 of 1", leave confirm, draw reasons, migration v3). E: 44 (Play → full game vs Mouse → 3 moves, bot < 3 s → leave → abandoned record; a11y walk scans full-game screens). Lead review: "full" game detection was hard-coded to `first-game` (would miss later full-game bosses) → rule "kings + standard start", test added; Play / My Den Owl row squeezed on phones → stacked (UI rule added to screens.md). S: Play vs Computer, full game mid-game check, win and draw results, Journey World 2 labels at 1024×768 and 390×844. Z: 172.5 KB gz |
| `m3` | 2026-09-24 | M3 Worlds 3–4 done | E CI A N P | Exit: Worlds 3–4 (10 lessons, 84 exercises, all answers rule-verified at build), 7 mini-games + 2 world bosses, review scheduler + Today + Practice, first checkmate reachable (Mate in 1 lesson, first full game vs Mouse, full games in Play). Pending (user): playtest 3 (and 1–2); Pages source = GitHub Actions |
| `deploy` | 2026-09-24 | First Pages deploy (owner set Pages source = GitHub Actions) of `m3` (131bfa8) | D | Deploy workflow run 23 success; environment URL https://enricoag1982.github.io/learningChess/. Page not fetched from the sandbox (proxy blocks github.io); first real-device check = playtest |
| `fix` | 2026-09-24 | Playtest (owner, live app): squares-04 "Tap every light square in the row closest to you" failed for correct-looking taps | N U E | Cause: text ambiguity, not code — king on d4 made "closest to you" read as "squares next to the king" (owner tapped d5, c4, e4, d3). Fix: squares-04 / 05 without the king, text "bottom row, the one nearest you"; loader allows piece-free select-squares with explicit squares (test added); review rule added to CLAUDE.md (no distractor pieces, no "closest to you"). E: whole-curriculum a11y walk hit its 150 s budget under load → 300 s. Owner request: on a wrong check also show the missed squares → dashed orange frames (wrong picks solid), markers clear per square once fixed; U: core 393, content 223, web 175 (marker labels + classes); E: 44; S: owner's case (d5 c4 e4 d3 → Check) on Pixel 7 emulation reviewed |
| `m4.1` | 2026-09-24 | World 5 (Full Rules): Castling (8), En Passant (5), Draws (5) with rule-verified answers (can-castle, castle, en-passant, can-en-passant, insufficient-material, draw-kind); `lastMove` highlight for the opponent's double step; world boss full game vs Rabbit | F L T U B E CI S A N Z P | U: core 404, content 258 (World 5 playthrough, fixture per verify rule), web 177. E: 48 (castling lesson end to end, en passant last-move tint, a11y walk through World 5). P: fox stand-in vs Rabbit 70% wins, 0% losses (rest draws: move limit / insufficient material) → floor 60%; kid never loses to a stronger stand-in. N (lead audit): castling-01 repeated the guided board → pawns added; en passant yes-no items said "just rushed" without showing it → `lastMove` on g2 / 04, and 05 shows the king as last move; draws-03/04/05 questions gave the answer away → neutral wording. S: castling, en passant with last move, draw-kind choice, Rabbit boss start at 1024×768 and 390×844. Z: 174.3 KB gz |
| `m4.2` | 2026-09-24 | Fox, Wolf, Bear playable (unlock: 3 full-game wins vs the level below, or any own-level win), aids per level (take back unlimited / 3 / none, danger ring), automatic suggested level (≥ 4 of last 5 won → suggest next; ≤ 1 → drop one), opening book (`bot-book.yaml`, Fox+), Bear search: transposition table, killer moves, quiescence, 250 ms iterative deepening | F L T U B E CI S A Z P | U: core 441, content 267, web 184 (level chips, suggested level, aids, book validation). E: 50 (Fox unlocked from records → full game vs Fox, 2 moves, bot < 3 s). P: Bear per move p50 ≈ 1850 → 280 ms, p95 ≈ 4080 → 320 ms (local); calibration (15 games / pairing): fox vs rabbit 93%, wolf vs fox 87%, rabbit vs mouse 40% (0 losses), bear vs wolf 27% → follow-up F4. Agent fixes: search changes altered Fox's moves (first-game winnability 80 → 75%) → Bear-only; Play "full game" gated on Mouse instead of the selected level. S: Play card (Fox suggested, Wolf / Bear locked), full game vs Fox with take-back counter at 1024×768 and 390×844. Z: 177.3 KB gz |
| `m4.3` | 2026-09-24 | vs Friend, same device: setup sheet (second player — another profile or Guest — game limited to Full Game / Pawn Wars / Win the Queen once unlocked for the active profile, board mode, legal-move dots, swap colours), face-to-face (fixed orientation, top side's pieces + per-player strip rotated 180°) and pass-and-play (board flips to the mover) board modes, per-player Take back (asks the player now to move) + Stop, result names the winner by nickname; one `GameRecord` per profile involved, guest excluded, no stars/mastery/review effect; My Den "Games with friends" count | F L T U B E CI S A Z | U: core 459 (+18: `domain/game/local-match.ts` 9, `app/friend-play.ts` 9), content 267 (unchanged), web 195 (+11: `Board` `rotateTopPieces`/`showLegalMoveDots` 4, `FriendPlay.test.tsx` 7). E: 54 (+4: `friend-play.spec.ts` × 2 tests × chromium/tablet — two profiles seeded World 4 mastered → full game pass-and-play → Scholar's mate by White → result names Mia → both profiles' records; board bounding box square, `|w−h| <= 2px`, at 1024×768 face-to-face, 768×1024 (same match, resized) and 390×844 pass-and-play; a11y walk extended to the setup sheet and a pass-and-play game, incl. take-back ask and stop confirm). Storage schema unchanged (`GameRecord` already had `opponent`; no new field). Lead review caught the board not staying square (per-player strips' variable height broke `aspect-square` under simultaneous `w-full h-full`); fixed with a small `SquareArea` wrapper (`ResizeObserver`-measured `min(width, height)`, explicit pixel size — `GameLayout`'s own viewport-tuned CSS caps assume a panel at least as tall as the board, not this screen's short strips), covered by the new squareness assertion above. S (re-shot after the fix): setup sheet, face-to-face at 1024×768 landscape and 768×1024 portrait, pass-and-play at 390×844 (before and after a move), result (Scholar's mate, both strips) reviewed against the sketch (`project/Friend.dc.html`) — deviation: single combined per-player strip (avatar + turn + Take back + Stop / result) top and bottom rather than the sketch's three separate columns (left Take-back / centre turn pill / right icon-only exit), and a labelled "Stop" button rather than an icon-only "X", per the app's own icon+label rule (`docs/screens.md` §1); black-piece rotation logic (flip by colour, not by board row) matches the sketch exactly. Z: 180.6 KB gz Lead review: friend-game board was not square (tablet 950×510, phone 355×700, pieces clipped) → sized to min(width, height), e2e asserts square at 1024×768, 768×1024, 390×844. |
| `m4.4` | 2026-09-24 | Badges (25 MVP badges in `badges.yaml`, pure engine evaluated after exercise / lesson / game / session events, max 2 celebrations per session, My Den badges grid with spoken conditions, locked = grey + lock), streak (1 free skip per ISO week, flame pill on Home ≥ 2 days), session log; storage v4 | F L T U B E CI S A Z | U: core 500, content 287 (badges.yaml validation), web 213 (celebration limit, Den grid, streak pill). E: 56 with M4.3 (Perfect Lesson celebration → My Den, axe-clean). Lead review: Queen Keeper assumed the kid is White (wrong for friend games as Black) → `GameRecord.color`, replay from the profile's side, test; friend games did not trigger the badge check → `recordLocalMatch` calls `checkRewards`; celebration Owl row squeezed → always stacked in dialogs (screens.md rule). S: My Den badges + streak, celebration, Home streak pill at 1024×768 and 390×844. Z: 186.8 KB gz |
| `m4.5` | 2026-09-25 | Test-out ("Show you know it": tap a locked lesson/world → sheet → runner reusing the review task runner, no hints, no easier variant — lesson 5 tasks / world 8, ≥1 per lesson; ≥80% first try masters the scope, unlocks it, boss stays available unwon; fail = no penalty), placement test (offered once right after creating a new player; 4 tasks per Basics world in order, ≥3/4 passes it, stops at the first failed world, skippable any time), parent unlock (Parent area → per-child "Unlock lessons & worlds" list, `masteredVia: 'parent'`, no star floor); every run records an `AssessmentResult`; a pass/unlock also runs `checkRewards` (milestone badges fire); storage v5 | F L T U B E CI S A Z | U: core 523 (+23: `domain/assessment.ts` 15, `app/assessment.ts` 8), content 287 (unchanged), web 227 (+14: `assessment-flow.test.tsx` 8 incl. sheet/runner-without-hints/pass-fail/placement offer+skip/parent unlock, `local-assessment-repository.test.ts` 4, migration v4→v5 2). E: 62 (+1: `assessment.spec.ts` — new player → placement passes World 1 / fails World 2 (no penalty) → Journey shows World 1 mastered, World 2 available → test-out of a still-locked lesson passes; a11y walk gained a dedicated M4.5 scan of the sheet, runner and result screen on all 4 viewports). Found + fixed while testing (pre-existing, not M4.5 logic): `Board.tsx` leaves a piece selected after an illegal-move rejection (deliberate — lets the kid retry immediately); the shared e2e `tapIllegalMove` helper didn't deselect it before the next real move, causing intermittent "Tap X first" failures on movement-type wrong-answer flows — fixed by tapping the piece's own square again first, the same pattern already used for `select-squares`. S: test-out sheet, runner, pass/fail results, placement offer, placement runner, parent unlock list at 1024×768 and 390×844 — Owl row stacked in every new dialog/sheet (screens.md §1), no Hint button in the runner, kid-sized (≥64px) touch targets throughout, no red/error styling on fail. Z: 190.4 KB gz Lead review: placement offer showed Owl twice → one Owl (bubble); test-out result used a small underlined "Say it again" link (< 64 px) and the placement summary had no spoken text → standard Owl row (bubble + replay button, stacked). |
| `m4` | 2026-09-25 | M4 World 5 + Play done | E CI A N P | Exit: full legal game incl. castling and en passant (World 5 lessons + full games vs Mouse–Bear), siblings play each other on one device (vs Friend, e2e Scholar's mate between two profiles), badges + streak, test-out + placement, parent unlock. Known: Bear weaker than Wolf (F4). Pending (user): playtests 1–3 |
| `m5.1` | 2026-09-25 | Parent area rebuilt as Overview → child report → child settings, plus a Backup screen: per-child report (progress by world, concept accuracy + weak list, minutes/day last 14 days, last 10 games/assessments, badges), settings (daily limit, voice/sound/hints, computer level, piece style — voice/hints/computer level take effect at the next profile select), rename/avatar/delete moved here from the old flat row, M4.5's unlock panel integrated (not duplicated), Reset (parent-password-confirmed, keeps identity), backup export (all or one child) / import (preview, atomic replace, clear error on an invalid/too-new file) | F L T U B E S A Z | U: core 577 (+54: `profile-settings.ts` 9, `settings.ts` 8, `report.ts` 10, `backup.ts` 17, `rewards.ts`/`profiles.ts`/`session-log.ts` additions), web 250 (+15: `parent-area-flow.test.tsx` 9, `local-backup-importer.test.ts` 4, `gated-narrator.test.ts` 5, plus `ExerciseStep`/`local-rewards-repository`/`local-settings-repository` additions — some net against 2 old flat-row tests rewritten for the new navigation). E: 29 (+1: `parent-area.spec.ts` — two children seeded → Overview → report shows seeded stars/games by name → export (real download) → reset (wrong password rejected, right one clears it) → import that same file → data back; every other spec updated for the new Overview→report→settings navigation still green). Found + fixed while testing (not this milestone's core logic): the Overview's per-child stats only refetched when the `profiles` array reference changed, so returning to it after a Settings-screen reset or a Backup import showed stale numbers — now also refetches on every return to the Overview view. A `zod` dependency was added to `packages/core` (previously content-build-only) for backup-file validation, a use-case-level concern on untrusted, user-supplied JSON. `AppSettings` gained a required `profileSettings` field (no storage version bump, same pattern `suggestedLevels` set in M4.2) — every pre-M5.1 `AppSettings` literal in the test suite needed it added. S: overview, report, settings, backup at 1024×768 and 390×844 — a `sm:` breakpoint added to the report's per-world row (title above stats) after phone-width wrapping looked cramped on first review. A: onboarding/profile a11y walk extended through report → settings → backup (rename, avatar, daily-limit chips, voice/sound/hints toggles — `role="switch"`, computer-level/piece-style chips, export/reset/delete), no serious/critical violations, all ≥ 44 px. Z: 247.8 KB gz (budget 300) Lead review: backup validation (zod) was re-exported from core's main entry → pulled into the main bundle and the bot worker (worker 16 → 42 KB gz) → backup values moved to the `@chess-kids/core/backup` subpath (worker 16.6 KB, total 223 KB gz). E: 64 (full run, 4 projects). CI e2e fail (placement, ~1 in 6 locally): `rook-07`/`queen-07` share one instruction text → e2e matcher picked the rook task on the queen board → `shownExercise` (e2e helpers) also compares starting pieces; per-task counter wait; placement test 16/16 repeats green. CI e2e fail 2 (friend board 342×692 on phone): `SquareArea` unsized until its first `ResizeObserver` callback (a frame after mount) → first size measured in the layout effect, before paint; friend-play 40/40 repeats green. |
| `m5.2` | 2026-09-25 | Daily time limit: activity gate checked between activities only (entering a lesson/warm-up/practice run/game/mini-game, and returning to Home — an open activity always finishes), "See you tomorrow" screen (Owl, spoken, today's stars) with **Switch player** / **Parent: more time** (password → +15 min today, repeatable), foreground time tracker (visibility + 2-min-idle aware, one minute per real minute, replaces the old Today-session-only lump sum — now every kid-mode screen counts), parent report's minutes-per-day chart gains a limit line | F L T U B E CI S A Z | U: core 602 (+25: `session-log.ts` 13, `time-limit.ts` 8, `report.ts` 2, `rewards.ts` `starsToday` 2), content 287 (unchanged), web 278 (+13: `TimeTracker.test.tsx` 6, `time-limit-flow.test.tsx` 5, `parent-area-flow.test.tsx` +2). E: `time-limit.spec.ts` (new, 6 tests: gate blocks → "See you tomorrow" → parent password → +15 min → lesson resumes; wrong password stays on the gate; Switch player → picker; never interrupts an open lesson, only re-checked on Close; limit off never gates; report's limit line) + a walk added to `a11y.spec.ts`'s onboarding test (touch targets + axe on the "See you tomorrow" screen and its password sub-flow); full suite 84 passed, 4 projects (chromium/tablet/tablet-portrait/phone). Deviation from the M5.1-era doc: `app-structure.md`'s time-controls table had "+15 min today" and parent "more time" filed under v2 — pulled into v1 per this task's own decision table (parent-initiated from the gate screen, password required, repeatable); kid-initiated "Ask for more time" and date overrides stay v2. Design choice: minutes are now tracked continuously for any kid-mode screen (`ui/TimeTracker.tsx`, mounted once in `App.tsx`), not only inside a Today session as before — the old `todaySessionStartedAt`/lump-sum recording is removed, matching the decision table's own "Home/Journey/Den browsing counts too". S: "See you tomorrow" + the "Parent: more time" password screen at 1024×768 and 390×844 reviewed. A: onboarding a11y walk extended through the gate screen, kid-sized (≥ 64 px) touch targets, no serious/critical violations. Z: initial 177.0 → 177.9 KB gz (budget 300), total 232.8 → 233.7 KB gz. Lead review: `TimeTracker`'s minute interval restarted on every screen change (short Home/Journey visits never counted) → interval per profile, screen read at each tick; RTL test red → green. |
| `m5.4` | 2026-09-25 | Offline + performance hardening: persistent storage request (once ever per device, `navigator.storage.persist()`), iPad install banner (Home, iOS Safari only, not standalone, dismiss remembered), lazy-loaded parent area / friend setup / friend game / placement offer / placement / assessment screens (`React.lazy` + `Suspense`, small Owl spinner fallback), CSP meta tag (production build only via a build-only Vite plugin), cold-start e2e (4× CPU throttle, tablet), offline e2e extended to a lazy screen + a World 5 exercise, Bear strength (roadmap F4: null-move pruning + late move reductions + history-heuristic move ordering, Bear only) | F L T U B E CI S O Z | U: core 523 (unchanged — Bear changes covered by the existing `search.test.ts` suite + calibration, per this task's own spec), content 287 (unchanged), web 241 (+14: `install-banner.test.ts` 7, `persistent-storage.test.ts` 5, `HomeScreen.test.tsx` +2 install-banner cases). E: 70 (+8 over m4.5's 62: `offline.spec.ts` +1 — parent area (lazy, never fetched online this session) + World 5 `castling` guided tries + exercise, all offline; `performance.spec.ts`, new — lazy chunk is its own network fetch, no `securitypolicyviolation` across Home/Journey/Play/Den/parent area, cold start logged + asserted < 5 s CI). Found + fixed while testing (not pre-existing, both introduced by this milestone's own lazy-loading): `assessment.spec.ts`'s shared `runTasks` helper checked candidate visibility with a single non-retrying `isVisible()`, racing `AssessmentScreen`'s new lazy chunk on the test-out flow's very first task — now retries for up to 5 s on a task's first check only; `performance.spec.ts`'s own CSP-flow test matched `getByRole('button', { name: 'Play' })` against both the "Play" tile and "Switch player" (a role-name substring match) — `exact: true` added. Deviation found mid-build: `style-src 'self'`-only (no `unsafe-inline`) would have broken every existing use of React's own `style` prop (avatar/board colours etc.) across this codebase, not just new code — CSP shipped with `'unsafe-inline'` on `style-src` only (per the spec's own "only if Tailwind / React need it" allowance), `script-src` stays plain `'self'`. Calibration (`pnpm --filter @chess-kids/core calibrate 30 bear`, same seeds, before vs after): bear vs wolf 13.3% → 20.0% (4/30 → 6/30), a real ~1.5× gain, still short of the 70% target and below M4.2's own 26.7%/15-seed figure — reported and stopped there per the task's own explicit allowance (`docs/computer-opponent.md` §6.6); an initial small-sample (N=10) self-play check looked like null-move pruning was a regression, which a same-seed, same-N baseline run showed was noise, not a real effect — kept it after re-measuring at N=30. Bear's own reference-set performance unaffected: p50 ≈ 277 ms, p95 ≈ 307 ms (existing ≤ 300 ms(local)/≤ 600 ms(CI) bounds hold, same range as M4.2's own 270-330 ms). Cold start (4× CPU throttle, tablet 1024×768, warm cache, picker tap → Home): 378-486 ms across 4 local e2e runs, well under the 3 s target. Z: initial JS 179.2 → 174.4 KB gz (budget 300; lazy screens' own code no longer eagerly modulepreloaded — `characters.tsx` stays eager since Home/Lesson/Journey also use it, so the win is real but modest); total (incl. lazy chunks + bot worker, informational only) 195.8 → 200.8 KB gz. O: parent-area (lazy) chunk fetched and rendered while offline; World 5 `castling` guided tries + one exercise solved offline, stars saved across reload. S: iPad install banner (1024×768, iOS UA) at Home; lazy-load Owl spinner at 1024×768 and 390×844 (network-delayed chunk fetch, to actually catch the fallback on screen). Port note: `pnpm test:e2e`'s shared preview port (4173) collided repeatedly with the parallel M5.1 agent's own e2e runs; waited and retried each time rather than killing anything, per this task's own instruction — eventually green. Coordination note for the lead/M5.1: `local-settings-repository.ts` (M5.1's own file) needs its `normalize`/`isAppSettingsShape`/`DEFAULT_SETTINGS` whitelist extended to round-trip the new `AppSettings.storagePersisted` field through a `get()` — `save()` already stores it unfiltered; flagged in `ports.ts`'s own doc comment on the field. Lead review (integration on M5.1): settings repository whitelist extended with `storagePersisted` + round-trip test; 2 parent-area RTL tests now await the lazy screen's child cards (`findByText`). Integration e2e found a CSP `script-src: eval` violation on the parent area: M5.1's zod (backup) probes `new Function` → `z.config({ jitless: true })` in `backup.ts`; CSP + parent-area specs green. After integration: U core 577, content 287, web 265, E 72; Z initial 177.0 KB gz, total 232.8 KB gz (parent area chunk 31.8 KB incl. backup zod). F4 stays open (Bear 20% vs Wolf). |
| `m5.5` | 2026-09-25 | Privacy policy (parent area "Privacy" row + first-run link, `docs/privacy-policy.md`), README, 9 easier variants (World 1–2, F2), release checklist (`docs/release.md`), app version shown in parent area | F L T U B E N S A Z | Variants: `squares-05-easy`, `lines-06-easy`, `setup-05-easy`, `bishop-08-easy`, `king-06-easy`, `knight-08-easy`, `pawn-08-easy`, `promotion-03-easy`, `queen-08-easy` — one per remaining World 1–2 lesson. `king-06-easy` authored stars3=3, corrected to 2 (King capture rules apply even inside `collect-stars`); caught by the content build. Found + fixed: `PrivacyDialog`'s scrollable content tripped axe `scrollable-region-focusable` → `tabIndex={0}`, found by the a11y e2e walk. U: core 577, content 287, web 267 (+2). E: 74 (+2), all green incl. 4-viewport a11y walk through the new Privacy screens. Z: 178.2 KB gz initial (budget 300), 234.0 KB gz total. N (lead, 9 variants, text vs board): 6 OK; `lines-06-easy` "this shorter diagonal" (g7 is on 2) → "the short one"; `setup-05-easy` "four pawns" (which?) → "in front of each piece on the left, rook to queen"; `squares-05-easy` "your left-hand corner" → "bottom-left corner"; `king-06-easy` started in check (pawn b2 attacks a1) in a "stay safe" lesson → king a1, pawn c3 guarding b2, star c2 (one safe 2-move path). `release.md`: `m5` comes with the last run iteration (M5.3), not M5.5. S (lead): phone privacy dialog — contact URL overflowed its panel → `wrap-anywhere`; re-shot at 390×844, Close in view. After integration on M5.2: U core 602, content 287, web 281; E 86; Z initial 179.2 KB gz, total 235.1 KB gz. |
| `m5.3` | 2026-09-25 | Design + accessibility pass: tappable vs info (roadmap F3) app-wide, classic pieces from World 5 + full games | F L T U B E S A Z | Design + accessibility pass (roadmap F3): `.tap-raised`/`.info-flat` tokens (`index.css`) + `--color-ledge-*`; shared primitives `TapButton`/`InfoPanel`/`InfoPill` (`ui/primitives.tsx`); parent area's own M5.1 tokens now built on the same system, not a second one. Applied across Home, Journey, Play, My Den, lesson, parent area, Time limit, Privacy. Classic pieces from World 5 and full games, parent `pieceStyle` override (`ui/board/piece-style.ts`). U: core 602, content 287, web 302 (+21: primitives, piece-style, Board badges). E: 86 (4 projects); `a11y.spec.ts` gains `expectOnlyButtonsRaised`, sampled at Home/Play/My Den/an exercise/parent overview-report-settings. S: before/after, 1024×768 and 390×844, all 8 required screens plus a seeded Full Game. Z: 180.5 KB gz (budget 300). Scope: board squares, Journey map nodes, avatar swatches and structural section panels keep their existing look, deliberately not raised. Lead review (S): lesson step row (Story · Demo · …) was still bold one-word pills with a solid current fill → flat progress track (label + bar); phone phase chip → flat tinted label; parent overview/report rank pill kid-sized in a list row → `compact`; README `lesson.png` / `parent-area.png` re-shot. `pieceStyle` reading accepted: `animal` (default) = badges except World 5 / full games, `classic` = classic everywhere. |
| `m5` | 2026-09-25 | M5 release complete: parent area, time limit, design/a11y, offline/perf, release docs; tags m5.1–m5.5 | CI | Exit: parent area (overview/report/settings, backup, privacy), daily time limit, app-wide tappable-vs-info + classic pieces from World 5, offline/performance hardening, release docs (README, privacy policy, release checklist). Known: Bear still short of Wolf (F4, open). Pending (user): playtests 1–4 |
| `v1.0.0` | 2026-09-25 | First release (MVP), same commit as `m5` | CI | `docs/release.md` §1: CI green (`quality`, e2e 86 incl. offline, CSP, cold start, backup / restore, a11y walk); version 1.0.0 shown in the parent area; privacy page in app + `docs/privacy-policy.md`. Pending (user): offline check on an iPad and an Android tablet, playtest 4. |
